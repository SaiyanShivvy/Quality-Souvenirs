@model IEnumerable<QualitySouvenirs.Models.OrderItem>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderItemPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderItemPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderItemID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderItemID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderItemID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
